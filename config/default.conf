# ZeroTier Gateway 默认配置文件
# 由配置管理系统自动生成和维护

# 接口配置
ZT_INTERFACE=""
WAN_INTERFACE=""
ZT_MTU="1350"
ZT_NETWORK=""
ZT_NETWORK_ID=""

# 功能设置
IPV6_ENABLED="0"
GFWLIST_MODE="0"
DNS_LOGGING="0"
DEBUG_MODE="0"

# 运行模式
RESTART_MODE="0"
UPDATE_MODE="0"

# 脚本版本
SCRIPT_VERSION="3.1"

# 配置文件版本
CONFIG_VERSION="2.0.0"

# 最后更新时间
LAST_UPDATE=""

# 高级选项
AUTO_UPDATE_GFWLIST="1"
BACKUP_CONFIGS="1"
LOG_LEVEL="INFO"

# 模板变量替换值
GENERATION_TIME=""  # 将在运行时设置

# 内核参数配置
CONNTRACK_MAX="131072"
TCP_TIMEOUT_ESTABLISHED="7200"
UDP_TIMEOUT="60"
UDP_TIMEOUT_STREAM="180"
SYN_BACKLOG="4096"
RMEM_MAX="16777216"
WMEM_MAX="16777216"
TCP_RMEM="4096 87380 16777216"
TCP_WMEM="4096 65536 16777216"
TCP_CONGESTION_CONTROL="bbr"

# ipset 配置
IPSET_TIMEOUT="86400"
DEFAULT_DNS_PRIMARY="223.5.5.5"
DEFAULT_DNS_SECONDARY="223.6.6.6"
FALLBACK_DNS_1="8.8.8.8"
FALLBACK_DNS_2="1.1.1.1"

# 高级网络优化设置
ADVANCED_NETWORK_OPTIMIZATION="0"  # 0=禁用, 1=启用
ENABLE_BBR_CONGESTION_CONTROL="1"  # 0=禁用, 1=启用BBR
ENABLE_FAST_OPEN="0"  # 0=禁用, 1=启用TCP Fast Open

# 系统服务配置
ENABLE_AUTOSTART="1"  # 0=禁用, 1=启用开机自启
LOG_RETENTION_DAYS="7"  # 日志保留天数
BACKUP_RETENTION_DAYS="30"  # 备份保留天数
